<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlanejamentoDeViagem.LoginPage"
             Title="Login">
    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="Login" FontSize="24" HorizontalOptions="Center" Margin="0,20,0,20"/>
        <Entry x:Name="TxtUsername" Placeholder="Usuário"/>
        <Entry x:Name="TxtPin" Placeholder="PIN" Keyboard="Numeric" IsPassword="True"/>
        <Button Text="Login" Clicked="OnLoginClicked" />
    </VerticalStackLayout>
</ContentPage>
